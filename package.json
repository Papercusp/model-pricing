{
  "name": "@papercusp/model-pricing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical model→price table + cost estimation for agent usage telemetry (cross-backend-cost-capture). Single source of truth for claude/codex/openai list prices; consumed by the orchestrator (cost cap, usage samples), operator-core (telemetry, insights), and testing-shell (re-export). Dependency-free.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
